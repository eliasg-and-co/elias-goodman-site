.vercel
*.mp3
# the two folders the site loads audio from must reach the deploy
!stems/*.mp3
!sfx/*.mp3
.env*
# raw camera/screen captures before trim+loudnorm+transcode; only the
# processed previews/*.mp4 output needs to reach the deploy
visual-assets/
